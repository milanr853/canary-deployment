FROM node:22-alpine
WORKDIR /workspace
COPY package.json package-lock.json* ./
RUN npm ci
# mount the workspace via docker-compose for HMR
COPY . .
EXPOSE 5173
ENV CHOKIDAR_USEPOLLING=true
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
